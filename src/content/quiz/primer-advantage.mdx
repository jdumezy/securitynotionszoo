---
type: "multiple-choice"
choices:
  - "$\\Pr[b' = b]$"
  - "$\\Pr[b' = b] - \\Pr[b' \\ne b]$"
  - "$|\\Pr[b' = b] - 1/2|$"
  - "$\\Pr[b' \\ne b]$"
correct: 2
explanation: "For indistinguishability games, the baseline is a random guess: $\\Pr[b' = b] = 1/2$. The advantage measures how much better than random the adversary performs, so it is the absolute deviation from $1/2$: $|\\Pr[b' = b] - 1/2|$."
tags: ["primer", "definition"]
---

For an **indistinguishability** security game, how is the adversary's advantage defined?
