---
title: "Homomorphic Chosen Ciphertext Attack"
acronym: "HCCA"
description: "CCA notion for univariate homomorphic schemes using a rigged-ciphertext game structure instead of two-plaintext challenges; generalizes IND-CCA, gCCA, and RCCA."
tags: ["Filtered adaptive decryption oracle", "FHE-compatible", "Univariate only"]
introduced_in:
  - title: "Homomorphic Encryption with CCA Security"
    authors: ["Manoj Prabhakaran", "Mike Rosulek"]
    year: 2008
    link: "https://link.springer.com/chapter/10.1007/978-3-540-70583-3_54"
implies: []
implied_by: ["vCCA"]
---

## Overview

Homomorphic CCA (HCCA) security, introduced by Prabhakaran and Rosulek, is a security notion for homomorphic schemes that only allow evaluation of **univariate** (single-ciphertext-input) functions, drawn from a transformation set $\mathcal{T}$. Unlike all other notions in this zoo, the challenge in the HCCA game consists of a *single* plaintext $m^*$ (not a pair), and the adversary distinguishes between a normal encryption of $m^*$ and a "rigged" ciphertext.

The game uses auxiliary procedures - RigEnc, RigExtract, and RigDec - to manage the distinction. A rigged ciphertext looks like a normal encryption but is constructed so that applying a homomorphic transformation $T \in \mathcal{T}$ to it yields a ciphertext that decrypts normally in the real world ($b=0$) or to $T(m^*)$ in the rigged world ($b=1$), recovered via RigExtract. The adversary's task is to determine which world it is in.

A central result of Prabhakaran and Rosulek (Theorem 1 of the paper) is that HCCA **generalizes** the standard indistinguishability-based CCA notions: when the transformation set $\mathcal{T}$ is the single identity function, HCCA coincides with IND-CCA2, and more generally HCCA subsumes IND-CCA, gCCA, and RCCA. HCCA is thus not a "weaker" CCA flavor aimed at making homomorphism possible; it is the natural generalization that allows non-trivial $\mathcal{T}$ while still ruling out all other malleability. For rich $\mathcal{T}$, however, HCCA is incomparable to these classical notions, because the classical notions would refuse transformations that HCCA legitimately allow.

HCCA is historically significant as one of the first attempts to define CCA-type security for homomorphic schemes, predating the SNARK-based vCCA approach by over a decade. Section 6 of the paper extends the definitions to **binary** (two-ciphertext-input) homomorphic operations and proves a negative result (Theorem 5): the natural generalisation of HCCA to binary operations is unachievable for a large class of useful homomorphisms. As a result, the HCCA framework as stated is limited to univariate evaluation, and this restriction is fundamental rather than cosmetic.

## Formal Definition

The HCCA game is defined relative to a subset $\mathcal{T}$ of univariate functions and relies on auxiliary procedures:

- $\mathsf{RigEnc}_{pk}()$: Returns $(c, S)$ where $c$ is a rigged ciphertext indistinguishable from a normal one, and $S$ is auxiliary information.
- $\mathsf{RigExtract}_{sk}(c', S)$: Determines if $c'$ was obtained by applying $T \in \mathcal{T}$ on $c$. Returns $T$ if so, $\bot$ otherwise.
- $\mathsf{RigDec}_{sk}(c)$: Depends on the challenge bit $b$.

The game proceeds as follows:

1. The challenger generates $(pk, sk) \leftarrow \mathsf{KeyGen}(1^\lambda)$ and gives $pk$ to $\mathcal{A}$.
2. **Phase 1:** $\mathcal{A}$ has access to a decryption oracle, $\mathsf{GRigEnc}_{pk}$ (which returns rigged ciphertexts), and $\mathsf{GRigExtract}_{sk}$ (which checks if a ciphertext is derived from a rigged one).
3. $\mathcal{A}$ outputs a plaintext $m^*$ and state $s$.
4. **Challenge:** The challenger samples $b \xleftarrow{\textdollar} \{0,1\}$. If $b = 0$: $c^* \leftarrow \mathsf{Enc}_{pk}(m^*)$. If $b = 1$: $(c^*, S_i) \leftarrow \mathsf{RigEnc}_{pk}$.
5. **Phase 2:** $\mathcal{A}$ has access to $\mathsf{GRigEnc}_{pk}$, $\mathsf{GRigExtract}_{sk}$, and $\mathsf{RigDec}_{sk}$, where:
   - If $b = 0$: $\mathsf{RigDec}_{sk}(c) = \mathsf{Dec}_{sk}(c)$.
   - If $b = 1$: $\mathsf{RigDec}_{sk}(c) = T(m^*)$ if $\bot \neq T \leftarrow \mathsf{RigExtract}_{sk}(c, S^*)$, and $\mathsf{Dec}_{sk}(c)$ otherwise.
6. $\mathcal{A}$ outputs a guess $b'$.

The scheme is HCCA-secure if the advantage $|\Pr[b' = b] - 1/2|$ is negligible for all PPT adversaries.

## Attacks & Relevance

HCCA prevents attacks where the adversary homomorphically transforms the challenge ciphertext and submits the result for decryption. In the rigged world ($b = 1$), such queries return the "expected" value $T(m^*)$ rather than the actual decryption, preventing information leakage through homomorphic manipulation.

The limitation to univariate evaluation means HCCA cannot model modern FHE settings where functions take multiple ciphertext inputs. This restriction is fundamental to the game structure: Theorem 5 of the paper shows that the natural generalization of HCCA to binary homomorphic operations is unachievable for a large class of useful homomorphisms, so the rigged-ciphertext approach does not straightforwardly extend.

## Achieving This Notion

Prabhakaran and Rosulek gave an explicit construction of an HCCA-secure scheme under the standard **Decisional Diffie-Hellman (DDH)** assumption. The construction is a careful generalization of the rerandomizable RCCA-secure scheme of Prabhakaran and Rosulek (TCC 2007) and supports the group operation, as well as several related operations, as its homomorphic feature. Notably, the construction does not require SNARKs or any other non-standard machinery, in contrast to later FHE-oriented notions such as vCCA.

## Further Reading

The HCCA notion was introduced by [Prabhakaran and Rosulek (ICALP 2008)](https://link.springer.com/chapter/10.1007/978-3-540-70583-3_54).
For a comparison with the vCCA approach, see [Manulis and Nguyen (Eurocrypt 2024)](https://eprint.iacr.org/2024/202).
For HCCA's place in the wider FHE security landscape, including its position as a notion implied by vCCA, see [Renard (PhD thesis, 2025)](https://theses.hal.science/tel-05421880v1/file/157007_RENARD_2025_archivage.pdf), §2.3.3 and Fig. 2.10.
